{
  "name": "link-smasher-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:extension": "pnpm --filter extension dev",
    "build:extension": "pnpm --filter extension build",
    "compile:extension": "pnpm --filter extension compile"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
